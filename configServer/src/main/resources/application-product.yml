spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://repos.e-lingcloud.com/component/com.eling.cloud.config.files.git
          username: wuxian
          password: wx.4489268..
eureka:
  client:
    serviceUrl:
      defaultZone: http://cloud-register.e-lingcloud.com/eureka/
server:
  port: 8080
logging:
  file: /opt/config-server.log